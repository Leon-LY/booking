# Nginx configuration for /booking sub-path
# Place at: /etc/nginx/sites-enabled/booking.conf

location /booking {
    proxy_pass http://127.0.0.1:3002/booking;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
}

# Deploy steps:
# 1. Create database:
#    docker exec -it investlearn-db psql -U postgres -c "CREATE DATABASE booking_app;"
#
# 2. Copy code to server
# 3. Update .env.production with correct DATABASE_URL and NEXTAUTH_SECRET
# 4. docker-compose up --build -d
# 5. Run migrations and seed:
#    docker exec -it booking-booking-app-1 npx prisma migrate deploy
#    docker exec -it booking-booking-app-1 npx prisma db seed
# 6. Add nginx config and reload:
#    sudo nginx -s reload
# 7. Visit http://49.232.49.175/booking
